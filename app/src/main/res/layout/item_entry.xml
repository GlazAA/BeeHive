<?xml version="1.0" encoding="utf-8"?>
<!-- Файл: item_entry.xml -->
<!-- Связан с: EntriesFragment.java, EntryAdapter.java -->
<!-- Назначение: макет отдельной карточки записи в списке -->
<!-- относится к EntryAdapter.java отвечает за список записей-->

<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="4dp"
    android:layout_marginHorizontal="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:strokeColor="@color/bee_accent"
    app:strokeWidth="1dp"
    app:cardBackgroundColor="@color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Верхняя строка: иконка типа, название и индикатор удаления -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Иконка типа записи (пароль или занятие) -->
            <ImageView
                android:id="@+id/ivTypeIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_marginEnd="8dp"
                android:src="@android:drawable/ic_menu_edit"
                android:tint="@color/bee_primary_dark"
                tools:src="@android:drawable/ic_menu_edit" />

            <!-- Название записи -->
            <TextView
                android:id="@+id/textTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textAppearance="@style/TextAppearance.MaterialComponents.Subtitle1"
                android:textColor="@color/bee_text_primary"
                android:textStyle="bold"
                tools:text="Название записи" />

            <!-- Индикатор мягкого удаления (для детей) -->
            <TextView
                android:id="@+id/tvDeletedIndicator"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="УДАЛЕНО"
                android:textColor="@android:color/holo_red_dark"
                android:textSize="12sp"
                android:textStyle="bold"
                android:visibility="gone"
                tools:visibility="visible" />
        </LinearLayout>

        <!-- Дополнительная информация (логин или время) -->
        <TextView
            android:id="@+id/textSubtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textAppearance="@style/TextAppearance.MaterialComponents.Body2"
            android:textColor="@color/bee_text_secondary"
            tools:text="Логин: user@example.com" />

        <!-- Информация о видимости и пароле (для родительского контроля) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:orientation="horizontal"
            android:visibility="gone"
            android:id="@+id/llPasswordInfo">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Пароль: "
                android:textColor="@color/bee_text_secondary" />

            <TextView
                android:id="@+id/tvPasswordMask"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="••••••••"
                android:textColor="@color/bee_text_primary" />
        </LinearLayout>

        <!-- Разделитель и метки (опционально) -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="8dp"
            android:background="@color/bee_accent"
            android:alpha="0.3" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tvAccessTag"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:paddingHorizontal="8dp"
                android:paddingVertical="2dp"
                android:text="Доступно всем"
                android:textColor="@color/bee_olive"
                android:textSize="11sp"
                android:background="@color/bee_green"
                android:backgroundTint="#33FFEC53" />

            <TextView
                android:id="@+id/tvTypeTag"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingHorizontal="8dp"
                android:paddingVertical="2dp"
                android:text="Пароль"
                android:textColor="@color/bee_primary_dark"
                android:textSize="11sp"
                android:background="@color/bee_primary"
                android:backgroundTint="#33FFEC53" />
        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>